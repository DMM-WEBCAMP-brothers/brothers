<%= render 'shippings/newform', member: @member, shipping: @shipping %>
<table class=“table”>
 	<thead>
		<tr>
		   	<th>郵便番号</th>
		   	<th>住所</th>
		   	<th>宛名</th>
		   	<th></th>
		</tr>
 	</thead>
 	<tbody>
 		<tr>
	   		<% @shippings.each do |shipping| %>
				<td><%= shipping.postcode %></td>
				<td><%= shipping.address %></td>
	     		<td><%= shipping.name %></td>
	     		<td>
	 				<%= link_to "編集する", edit_shipping_path(shipping), class:'col-xs-12 btn btn-default glyphicon glyphicon-wrench' %>
	       			<%= link_to "削除する", shipping_path(shipping), class:'btn-sm btn-danger',  method: :delete, data: {confirm: "削除しますか？"} %>
	     		</td>
	  		<% end %>
 		</tr>
	</tbody>
</table>