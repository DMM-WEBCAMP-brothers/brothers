<div class="genre-box">	
		<table class="table table-bordered table-hover">
			<tr class="text-center">
			 	<th class="text-center text-primary bg-success">
			 		<span class="glyphicon glyphicon-search" aria-hidden="true"></span>ジャンル検索
			 	</<th>
			</tr>
		 	<% @genres.each do |genre| %>
				<tr>
					<td class="bg-succes">
						<%= link_to products_path(genre_id:genre.id) do %>
							<p class="text-center "><%= genre.name %></p>
						<% end %>
					</td>
				</tr>
			<% end %>
		</table>
</div>
