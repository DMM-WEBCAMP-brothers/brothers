<h1>会員一覧</h1>
<div class="row">
	<div class="col-xs-6 col-xs-offset-3">
		<table class="table table-hover">
			<thead>
				<tr>
					<th class="success">会員ID</th>
					<th class="success">氏名</th>
					<th class="success">メールアドレス</th>
					<th class="success">ステータス</th>
				</tr>
			</thead>
			<tbody>
				<% @members.each do |member| %>
				<tr>
					<td><%= member.id %></td>
					<td>
						<%= link_to admins_member_path(member) do %>
							<%= member.last_name + member.first_name %>
						<% end %>
					</td>
					<td><%= member.email %></td>
					<td><%= member.is_deleted %></td>
				</tr>
				<% end %>
			</tbody>
		</table>
	</div>
</div>
